name: Angular CI avec Firefox

on:
  push:
    branches: # Déclenche uniquement sur push vers ces branches
      - master
      - develop # Adapte si tu utilises une autre branche principale de dev
  pull_request: # Déclenche aussi sur les Pull Requests vers ces branches
    branches:
      - master
      - develop

jobs:
  build_and_test_angular:
    name: Build & Test App Angular avec Firefox
    runs-on: ubuntu-latest

    env:
      NODE_VERSION: '20'
      APP_DIRECTORY: ./angular/maSuperAppAngular

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: ${{ env.APP_DIRECTORY }}/package-lock.json

      - name: Installer Firefox ESR pour les tests
        run: sudo apt -y update && sudo apt -y upgrade && sudo apt -y install firefox-esr

      - name: Installer les dépendances NPM
        working-directory: ${{ env.APP_DIRECTORY }}
        run: npm ci

      - name: Linter le code (Optionnel mais bonne pratique)
        working-directory: ${{ env.APP_DIRECTORY }}
        run: ng lint

      - name: Exécuter les tests unitaires avec Firefox Headless
        working-directory: ${{ env.APP_DIRECTORY }}
        run: ng test --watch=false --no-progress --browsers=FirefoxHeadless

      - name: Builder l'application pour la production
        working-directory: ./angular/maSuperAppAngular
        run: ng build --configuration production

      - name: Étape conditionnelle pour la branche master
        working-directory: ./angular/maSuperAppAngular
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        run: echo "🎉 Push sur master réussi et buildé avec Firefox !"
